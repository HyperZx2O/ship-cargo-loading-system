# Demo — run locally

Functional showcase of the final build. No build step.

```powershell
cd ship-cargo-loading-system/demo
python3 -m http.server 8000
# open http://localhost:8000
```

Or open `index.html` directly. Logic in `app.js`, tokens in `styles.css`.
